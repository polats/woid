/**
 * Back-compat re-export. Canonical home is agent-sandbox/woid-core/harness/moodlets.js.
 * Browser-side callers continue to pass `opts.persist` directly; the
 * createMoodletsTracker factory is unchanged.
 */
export * from "../../../agent-sandbox/woid-core/harness/moodlets.js";
