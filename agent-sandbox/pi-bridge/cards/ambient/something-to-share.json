{
  "id": "something-to-share",
  "phase": "ambient",
  "weight": 4,
  "intensity_min": 0,
  "intensity_max": 0.7,
  "cooldown_sim_min": 480,
  "roles": {
    "host": { "select": "random_character" }
  },
  "actions": [
    { "type": "Suggest", "target": "host", "text": "Something in front of you might be worth showing — small, mundane, specific. If it lands a `post` with an image_prompt, write it; otherwise let it pass." }
  ],
  "description": "Plants the impulse to make an image post. Solo (no scene-mate needed — posting is offstage). The character decides whether to act on it."
}
